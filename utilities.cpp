#include "utilities.h"

char** Utilities::fen_to_array(std::string fen) {
    int c = 0; // column
    int r = 0;  //row
    
    // for (i = 0; i < len(fen); i++) {
    //     while (fen[i] != ' ') {
    //         if (fen[i] == '/') {
    //             r += 1;
    //         }
            
    //     }
    // }
}